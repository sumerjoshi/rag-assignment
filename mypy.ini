[mypy]
# repo uses "from src...." imports without an src/__init__.py, so resolve
# modules from the repo root instead of treating files as top-level modules.
explicit_package_bases = True
namespace_packages = True
# llama-index ships incomplete type info, don't fail on its missing stubs.
ignore_missing_imports = True
