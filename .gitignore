# ---- Secrets / env (NEVER commit) ----
.env
.env.*
!.env.example
*.key
*.pem
*.crt
credentials.json
secrets.*

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.eggs/
develop-eggs/
pip-wheel-metadata/

# ---- Virtual environments ----
.venv/
venv/
env/
ENV/
.python-version

# ---- uv (keep uv.lock tracked; ignore caches) ----
.uv/
# uv.lock   <- intentionally NOT ignored; commit it for reproducible installs

# ---- Testing / linting / type-checking caches ----
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# ---- Jupyter / IPython ----
.ipynb_checkpoints/
profile_default/
ipython_config.py

# ---- Vector store / local indices (regenerated by the write/ingest path) ----
storage/
*.chroma/
chroma/
.chroma/
*.faiss
*.index
index_store/
vector_store/
.cache/

# ---- Logs / runtime ----
*.log
logs/
*.pid

# ---- IDE / editor ----
.vscode/
.idea/
*.swp
*.swo
*~

# ---- OS ----
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# ---- Assignment data (regenerable via ./setup.sh) ----
# Intermediate SEC inputs are rebuild-only (see data/README.md):
data/xbrl_raw/
# SQLite DB is regenerated by ./setup.sh (build_sqlite.py) — do not commit:
data/financials.db
*.db
*.sqlite
*.sqlite3
*.db-wal
*.db-shm
# Uncomment if you also want to keep the large PDFs out of git
# (they can be re-rendered with ./setup.sh):
# data/pdfs/

# ---- Vector store (regenerable via src/ingest/build_index.py) ----
# Embedded index is rebuilt from data/pdfs/ — do not commit:
storage/
